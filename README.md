# dotfiles

## Installation

Clone the repository:

```bash
git clone https://github.com/ilovescreammovies/dotfiles ~/dotfiles
```

Create the symbolic links:

```bash
cd ~/dotfiles && chmod +x ./symlink.sh && ./symlink.sh
```
